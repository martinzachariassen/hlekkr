# Docs

The [root README](../README.md) carries the architecture and the reasoning behind every
security decision. These pages cover each part in depth:

- [`backend.md`](backend.md) — the API: setup, configuration, and the contract it exposes
  to the frontend
- [`frontend.md`](frontend.md) — the web client: setup, configuration, and what it expects
  from the API and the Caddy proxy
- [`deployment.md`](deployment.md) — how the live site is deployed (Railway); the stack
  itself runs on any container platform
